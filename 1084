#include<stdio.h>
int main(){
	int x, y, z, sum=0;
	scanf("%d %d %d", &x, &y, &z);
	for(int i=0; i<x; i++)
	{
		for(int j=0; j<y; j++)
		{
			for(int k=0; k<z; k++)
			{
				printf("%d %d %d\n", i, j, k);
				sum+=1;
			}
		}
	}
	printf("%d", sum);
	return 0;
}
